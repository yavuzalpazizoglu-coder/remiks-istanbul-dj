{
  "name": "remiks-istanbul-dj-request",
  "version": "1.0.2",
  "description": "Remiks İstanbul - Canlı DJ İstek & Oylama Sistemi",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "node --watch server.js",
    "client:dev": "npm run dev --prefix client",
    "postinstall": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "NODE_ENV=production node server.js",
    "setup": "npm install && cd client && npm install",
    "tanitim:render": "node tanitim/render-tanitim-banner.mjs",
    "tanitim:social": "node tanitim/social/render-social.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.1.1",
    "nanoid": "^5.0.9",
    "nodemailer": "^8.0.4",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
